spring.application.name=sb-ecom

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
server.port=${PORT:8080}


#spring.datasource.url=${DB_URL:jdbc:postgresql://ep-hidden-dawn-aqoxmhzw-pooler.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
#spring.datasource.username=${DB_USERNAME:neondb_owner}
#spring.datasource.password=${DB_PASSWORD:npg_ZlqVLfgt1S6W}

spring.jpa.hibernate.ddl-auto=update



spring.jpa.open-in-view=false
project.image=images/

#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true


spring.app.jwtSecret=${JWT_SECRET}
spring.app.jwtExpirationMs=${JWT_EXPIRATION_MS:300000000}
spring.ecom.app.jwtCookieName=${JWT_COOKIE_NAME:springBootEcom}



#logging.level.org.springframework=DEBUG
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.springframework.security=DEBUG
#logging.level.com.ecommerce.project=DEBUG


# Needed if you are using elastic beanstalk
#server.port=5000
frontend.url=${FRONTEND_URL:http://localhost:5173/}
image.base.url=${IMAGE_BASE_URL:https://multi-vendor-e-commerce-platform-1.onrender.com/images/}
app.security.cookie-secure=${COOKIE_SECURE:false}
app.security.cookie-same-site=${COOKIE_SAME_SITE:Lax}
app.security.cookie-domain=${COOKIE_DOMAIN:}
app.security.cookie-max-age-seconds=${COOKIE_MAX_AGE_SECONDS:86400}
server.forward-headers-strategy=framework

#STRIPE KEY
stripe.secret.key=${STRIPE_SECRET_KEY:}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:}

#MAIL
app.mail.enabled=${MAIL_ENABLED:true}
app.mail.from=${MAIL_FROM:}
app.mail.support-name=${MAIL_SUPPORT_NAME:Marketplace Support}
app.mail.frontend-base-url=${FRONTEND_URL:http://localhost:5173}
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
